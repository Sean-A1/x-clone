# Supabase pooler connection — used by runtime (lib/db.ts) and drizzle-kit
DATABASE_URL=

# Supabase direct connection — reserved for future migrations (currently unused)
DIRECT_URL=

# Supabase project URL (public, safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anon key (public, safe to expose to browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
